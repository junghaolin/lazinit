# Lazinit TODO & 設計思路

## 🎯 核心理念
- **去中心化**：每台機器自己初始化，不依賴中控
- **輕量級**：不需要額外的管理服務或主控機
- **可攜帶**：只要有 git，任何機器都能快速部署

## 📋 待辦事項

### 高優先級

#### 1. 環境自動檢測
- [ ] 增強 `init_env.sh`，自動檢測環境類型
  - [ ] 檢測是否為 VM（hostname 規則或系統資源）
  - [ ] 檢測 OS（macOS vs Linux）
  - [ ] 檢測用途（work vs home vs learning）
- [ ] 根據環境自動選擇配置

#### 2. 包管理優化
- [ ] 創建分層的包列表
  - [ ] `init_apt_pks` - Linux 完整版
  - [ ] `init_apt_pks.vm` - VM 精簡版（去掉 btop, nvtop, glances 等重量級）
  - [ ] `init_brew_pks` - macOS 版本
- [ ] 處理 Ubuntu/macOS 工具名稱差異（bat vs batcat）

#### 3. 跨平台支持
- [ ] macOS 支持（brew 包管理）
- [ ] 處理路徑差異（/opt vs /usr/local）
- [ ] 工具名稱映射（batcat → bat on macOS）

### 中優先級

#### 4. 模組化改進
- [ ] 每個模組獨立安裝腳本可以單獨運行
  - [x] zsh/install.sh ✅
  - [ ] neovim/install.sh 增強（環境檢測）
  - [ ] ghostty/install.sh
  - [ ] utils/install.sh

#### 5. 工作環境特化
- [ ] 工作環境 proxy 設置
- [ ] 工作環境特定工具
- [ ] ~/.zshrc.local 自動生成

#### 6. VM 優化
- [ ] 資源受限檢測（RAM < 4GB）
- [ ] 精簡安裝模式
- [ ] 跳過非必要工具

### 低優先級

#### 7. 文檔完善
- [ ] 各模組的 README
- [ ] 使用場景文檔
- [ ] 故障排除指南

#### 8. 測試腳本
- [ ] 乾跑模式（--dry-run）
- [ ] 驗證腳本（檢查安裝是否成功）

---

## 💭 技術方案探討

### Chezmoi 集成（待評估）

**方案**：用 Chezmoi 管理 lazinit 本身

```
~/.local/share/chezmoi/
└── dot_lazinit/          # 整個 lazinit 作為子目錄
    ├── init_env.sh.tmpl  # 模板化
    ├── zsh/
    └── ...
```

**優點**：
- ✅ 保留 lazinit 結構
- ✅ 獲得模板功能
- ✅ 獲得加密功能

**缺點**：
- ❌ 增加複雜度
- ❌ 學習成本
- ❌ lazinit 本身就是要簡單

**結論**：暫時不採用，先把 init_env.sh 增強做好

---

### Ansible 對比（不採用）

**為什麼不用 Ansible？**

| 特性 | Lazinit | Ansible |
|------|---------|---------|
| 架構 | 去中心化 | **中央控制** ⚠️ |
| 依賴 | Git + Bash | 中控機 + SSH |
| 理念 | 每台機器自主 | 推送配置 |
| 適用 | 個人機器 | 團隊/大規模 |

**Ansible 的問題**：
- ❌ 需要一台「主控機」
- ❌ 需要配置 inventory
- ❌ 違背「去中心化」理念
- ❌ 新機器需要先設置 SSH 信任

**Lazinit 的優勢**：
- ✅ `git clone + ./init_env.sh` 即可
- ✅ 不需要其他機器配合
- ✅ 符合個人開發者使用習慣

---

## 🔄 演進路線

### 階段 1：環境感知（當前）
- 自動檢測環境並選擇配置
- 無需手動編輯腳本

### 階段 2：條件配置（近期）
- 根據環境生成 .local 配置
- 處理跨平台差異

### 階段 3：智能安裝（未來）
- 檢測已安裝工具，跳過
- 失敗重試機制
- 安裝後驗證

---

## 📝 筆記

### Chezmoi 使用筆記
- `~/.local/share/chezmoi/` 是 chezmoi 的源碼目錄
- `dot_` 前綴會轉換為 `.` 
- 可以把整個 repo 放進去，保持結構
- **但這不是 lazinit 的風格** - 我們要更簡單直接

### 現有問題
1. macOS 上無法直接用（apt 限定）
2. VM 會安裝很多不需要的重量級工具
3. 工作環境需要手動添加 proxy
4. 每台機器要手動調整配置

### 目標
- 一行命令完成 90% 的設置
- 剩下 10% 通過 .local 文件客製化
- 保持簡單，不引入複雜依賴

---

## 🎯 下一步行動

1. **本週**：增強 `init_env.sh` 的環境檢測
2. **下週**：創建分層包列表（完整/精簡/macOS）
3. **月底**：測試所有環境（macOS + Linux + VM）

---

*最後更新：2025-12-16*
